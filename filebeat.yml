filebeat.inputs:
  - type: container
    paths:
      - /var/lib/docker/containers/*/*.log
    processors:
      - add_docker_metadata:
          host: "unix:///var/run/docker.sock"
      - decode_json_fields:
          fields: ["message"]
          target: "json"
          overwrite_keys: true
          add_error_key: true

processors:
  - add_host_metadata: ~
  # Отфильтровываем только логи нашего бэкенда
  - drop_event:
      when:
        not:
          contains:
            docker.container.name: "sportgid"

output.elasticsearch:
  hosts: ["http://elasticsearch:9200"]
  index: "sportgid-logs-%{+yyyy.MM.dd}"

setup.template.name: "sportgid-logs"
setup.template.pattern: "sportgid-logs-*"
setup.ilm.enabled: false

logging.level: warning
